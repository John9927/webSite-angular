import { OrderingDirective } from './ordering.directive';

describe('OrderingDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderingDirective();
    expect(directive).toBeTruthy();
  });
});
